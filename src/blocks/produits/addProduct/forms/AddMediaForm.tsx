import React from 'react';

const AddMediaForm: React.FC = () => {
    return (
        <div>
            <h1>
                <span className="text-2xl font-bold">Ajouter des médias</span>
                <span className="text-sm text-gray-500"> (images, vidéos, etc.)</span>
            </h1>
        </div>
    );
};

export default AddMediaForm;