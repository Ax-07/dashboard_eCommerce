import { DashboardShell } from '@/src/blocks/dashboardBlocks/DashboardShell';
import React from 'react';

const VenteCAPage = () => {
    return (
    <DashboardShell>
      <section
        id="catalogue"
        className="relative flex-1 flex flex-col w-full p-4 items-center"
      >
      </section>
    </DashboardShell>
    );
};

export default VenteCAPage;